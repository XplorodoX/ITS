# AALeC Quiz — Mosquitto Broker Configuration
#
# TCP for firmware clients (ESP8266 / PubSubClient)
listener 1883
protocol mqtt

# WebSockets for beamer frontend (browser)
listener 9001
protocol websockets

allow_anonymous true
