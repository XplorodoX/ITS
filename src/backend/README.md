# Backend (Placeholder)

This directory is reserved for backend implementation.

Planned stack:
- Python (managed with `uv`)

Planned files (later phase):
- `pyproject.toml`
- `uv.lock`
- `game_master.py`
- supporting modules (`mqtt_client.py`, `scoring.py`, ...)

Current phase:
- Scaffolding and CI/DevOps preparation only
