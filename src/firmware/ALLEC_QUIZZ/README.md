# aAlec_quiz (Placeholder)

Reserved for the ESP8266 quiz sketch and helper modules.

Planned files (not created yet):
- `aAlec_quiz.ino`
- `config.h.example`
- `display.h/.cpp`
- `leds.h/.cpp`
- `mqtt.h/.cpp`

Note:
- `config.h` must remain local and never be committed.
