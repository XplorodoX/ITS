# Source Layout

This repository uses a source root with a clear split between backend and
frontend:

- `src/backend` for the Python game master and related backend modules
- `src/firmware` for ESP8266/AALeC firmware
- `src/frontend` for the Node.js/Next.js projector frontend

Current phase:
- Bootstrap and DevOps preparation only
- No production implementation yet
